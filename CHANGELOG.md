# 更新日志

## [2025-09-18] - 增加呼叫次数配置功能

### 新增功能
- **可配置呼叫次数**: 用户现在可以自定义语音呼叫的次数，不再固定为3次
- **持久化配置**: 呼叫次数设置会自动保存，应用重启后保持用户设置
- **用户友好界面**: 新增专门的输入框用于设置呼叫次数，支持数字输入验证

### 技术实现
- 添加了 `_callCount` 控制器来管理呼叫次数配置
- 集成到现有的 SharedPreferences 配置系统中
- 在 `initTts()` 方法中读取保存的配置，默认值为3次
- 在 `_save()` 方法中保存用户设置
- 修改 `_success()` 方法中的语音呼叫逻辑，使用配置的次数替代硬编码值
- UI界面新增呼叫次数输入框，带有中文标签和输入提示

### 使用说明
1. 在应用主界面找到"呼叫次数"输入框
2. 输入期望的呼叫次数（如：1、2、5等）
3. 点击"保存配置"按钮保存设置
4. 后续的语音叫号将按照新设置的次数进行呼叫

### 兼容性
- 保持向后兼容，默认呼叫次数仍为3次
- 输入验证确保无效输入时回退到默认值
- 不影响现有的其他配置功能

### 文件修改
- `lib/main.dart`: 主要功能实现
  - 新增呼叫次数配置变量和UI组件
  - 修改语音呼叫逻辑
  - 集成配置保存和读取功能
